import React from 'react';
import { SimpleInfoScreen } from './_components';

export default function NotificationsScreen() {
    return (
        <SimpleInfoScreen
            title="Notifications"
            subtitle="Manage alerts and emails"
            body="Notification preferences will be configurable here. You'll be able to choose booking, tutor, and promotional alerts once implemented."
        />
    );
}
